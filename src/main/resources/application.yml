server:
  port: 8081
management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
spring:
  datasource:
    username: root
    password: admin@235
    url: jdbc:mysql://localhost:3308/sample_db
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    open-in-view: false
    properties:
      hibernate.format_sql: true
keycloak:
  base_url: ${KEYCLOAK_BASE_URL:localhost:8087}
  realm: ${KEYCLOAK_REALM:basic-service}
  client_id: ${KEYCLOAK_CLIENT_ID:basic-client}
  client_secret: ${KEYCLOAK_CLIENT_SECRET:l6eLWm2uNsLi3bDStlnxyF4Rl57ePSKq}
logging:
  level:
    org.springframework: info
    #    org.springframework: debug
    org.springframework.security: debug